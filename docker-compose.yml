version: "3.8"
services:
  app:
    container_name: proshop-vita
    build:
      context: .
      dockerfile: .dockerfile
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - .env
    volumes:
      - .:/src
      - /src/node_modules
    command: npm run dev
